services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: reason
      POSTGRES_PASSWORD: reason
      POSTGRES_DB: reason
    ports:
      - "5432:5432"
    volumes:
      - reason_pg:/var/lib/postgresql/data

volumes:
  reason_pg:
